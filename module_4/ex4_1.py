length = float(input("How length is the zander in cm? "))
limit = 42 - length
if length < 42:
    print(f"Please  release the fish back into the lake. The fish is {limit} cm below the size limit caught fish.")

